# Perfil "nuvem": ativa autenticação JWT (ver ConfiguracaoSeguranca).
# Ativar com: java -jar financeiro.jar --spring.profiles.active=nuvem

financeiro.jwt.segredo=${JWT_SECRET}
financeiro.jwt.validade-horas=${JWT_VALIDADE_HORAS:720}
